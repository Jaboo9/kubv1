apiVersion: v1
Kind: Pod
metadata:
  name: worker-app-pod
  labels:
    name: worker-app-pod
    app: demo-voting-pod

spec:
  containers:
    - name: worker-app
      image: dockersamples/examplevotingapp_worker
